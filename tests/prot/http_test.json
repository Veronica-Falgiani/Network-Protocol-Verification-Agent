{
  "vulns": {
    "IS OPEN" : {
      "description": "Using HTTP instead of HTTPS can be dangerous when exchanging sensitive data",
      "severity": "low"
    },
    "PUT METHOD ALLOWED": {
      "description": "PUT method is enabled sitewide",
      "send": "PUT / HTTP/1.1\r\nHost: host:port\r\nConnection: close\r\n\r\n",
      "recv": "200",
      "severity": "low"
    },
    "DELETE METHOD ALLOWED": {
      "description": "DELETE method is enabled sitewide",
      "send": "DELETE / HTTP/1.1\r\nHost: host:port\r\nConnection: close\r\n\r\n",
      "recv": "200",
      "severity": "low"
    },
    "SHELLSHOCK": {
      "description": "We can use arbitrary headers to access the website",
      "send": "GET / HTTP/1.1\r\nHost: host:port\r\nUser-Agent: () { :;}; /bin/bash -c 'echo SHELLSHOCK!!'\r\n\r\n",
      "recv": "SHELLSHOCK!!",
      "severity": "high"
    },
    "HTTP STRICT TRANSPORT SECURITY HEADER MISSING": {
      "description": "The HTTP Strict Transport Security (HSTS) feature enables a web server to inform the user’s browser, via a special response header, that it should never establish an unencrypted HTTP connection to the specified domain servers. Instead, it should automatically establish all connection requests to access the site through HTTPS. This also prevents users from overriding certificate errors.",
      "send": "GET / HTTP/1.1\r\nHost: host:port\r\nConnection: close\r\n\r\n",
      "not_recv": "Strict-Transport-Security: max-age= ; includeSubDomains",
      "severity": "medium"
    },
    "NO SECURE FLAG IN COOKIE": {
      "description": "Using cookies and not setting the Secure parameter can be a major security vulnerability",
      "send": "GET / HTTP/1.1\r\nHost: host:port\r\nConnection: close\r\n\r\n",
      "not_recv": "Secure",
      "severity": "medium"
    },
    "NO HTTPONLY FLAG IN COOKIE": {
      "description": "If a cookies is used and not setting the HttpOnly parameter can be a major security vulnerability",
      "send": "GET / HTTP/1.1\r\nHost: host:port\r\nConnection: close\r\n\r\n",
      "recv": "HttpOnly",
      "severity": "medium"
    }
  },
  "login": {},
  "auth_vulns": {},
  "serv_names": [
    "apache"
  ]
}
